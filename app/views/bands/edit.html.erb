<%= render 'shared/navbar' %>

<div class="band-edit">

<% if flash[:success] %>
<p><%= flash[:success] %></p>
<% end %>

<h1> Edit your band page <h1>

<%= render 'form' %>

<%= link_to 'Back', band_path(@band) %>

</div>