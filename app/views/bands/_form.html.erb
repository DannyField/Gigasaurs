<div class="form-band">
<%= form_with model: @band, local: true do |form| %>
<div>
    <%= form.label 'BAND NAME :' %>
    <%= form.text_field :name %>
</div>

<div>
    <%= form.label 'ABOUT :' %>
    <%= form.text_field :about %>
</div>

<div>
    <%= form.label 'WEBSITE :' %>
    <%= form.text_field :website %>
</div>

<%# Allows a user to upload images to aws %>
<div>
    <%= form.label 'UPLOAD BAND IMAGE :' %>
    <%= form.file_field :profile_picture, accept: 'image/png, image/jpeg, image/jpg' %>
</div>

<%# This form was moved to the edit page, as errors were created when trying to add someone during the band creation stage %>
<%# <div> %>
    <%# <%= form.label :band_member %>
    <%# <%= form.text_field :band_member %>
<%# </div> %>

<div class="form-info">
</div>

<%# This works. It shows the users in the user table %>
<%# <%= form.collection_select(:user_id, User.all, :id, :name)  %>
<%# <% form.text_field :id, value: params[:user_id], class: "form-control-hidden" %>

<div>
<%= form.submit name="Create Band"%>
</div>
<% end %>

</div>