<div class="form-band">
<%= form_with model: @band, local: true do |form| %>
<div>
    <%= form.label 'BAND NAME :' %>
    <%= form.text_field :name %>
</div>

<div>
    <%= form.label 'ABOUT :' %>
    <%= form.text_field :about %>
</div>

<div>
    <%= form.label 'WEBSITE :' %>
    <%= form.text_field :website %>
</div>

<div>
    <%= form.label 'UPLOAD BAND IMAGE :' %>
    <%= form.file_field :profile_picture, accept: 'image/png, image/jpeg, image/jpg' %>
</div>

<div class="form-info">
<p>To remove. A user must create a band. That user must then add people to the band via email! </p>
</div>

<div>
    <%= form.label :band_member %>
    <%= form.text_field :band_member %>
</div>

<div class="form-info">
Incase there are people with the same name! So an email would be the best way.
Or a secret number created that shouldn't be shared with others. Like a membership number
</div>

<%# This work. It shows the users in the user table %>
<%# <%= form.collection_select(:user_id, User.all, :id, :name)  %>

<%# <% form.text_field :id, value: params[:user_id], class: "form-control-hidden" %>

<div>
<%= form.submit %>
</div>
<% end %>

</div>
USER CREATES BAND FIRST

EDIT AND ADDS PEOPLE TO THE BAND  