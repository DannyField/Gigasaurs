<%= render 'shared/navbar' %>

<% if flash[:success] %>
<p><%= flash[:success] %></p>
<% end %>

<div class="gig-edit">

<h1> Edit an existing listing <h1>

<%= render 'form' %>

<%= link_to 'Back', gig_path(@gig) %>

</div>